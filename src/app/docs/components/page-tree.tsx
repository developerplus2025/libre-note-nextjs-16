import type { PageTree } from "fumadocs-core/server";
import { source } from "@/lib/source";

export function PageTree({ tree, ...props }: { tree: typeof source.pageTree }) {
  console.log();
  return "";
}

